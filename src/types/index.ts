export type FrameTheme = {
  id: string;
  name: string;
  borderColor: string;
  accentColor: string;
  bgColor: string;
  labelColor: string;
};

export type PhotoSlot = {
  id: number;
  imageData: string | null;
};
